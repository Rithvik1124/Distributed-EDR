# To Do:
## 25/Aug/2026
- **main.rs:** line 261 - Automatic allocation of server IP
- **create a allocated role detection system in src/; which understands which role it has been provided, then runs those roles, if given consensus then central server ip is provided by the "scheduler", else a consensus node's ip is pinged by the "scheduler" #just do some placeholder thing for now - this needs to be done in CoordinationFabric thingy**.
- **create a client-server thingy for getting in telemetry and sending telemetry.**



## Things to keep in mind:
- Consensus nodes are the server for non-consensus nodes
- Central server is the server for consensus nodes